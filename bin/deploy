#!/usr/bin/env bash

set -eu

: ${DEPLOY_USER:="deploy"}
: ${DEPLOY_HOST:="127.0.0.1"}
: ${DEPLOY_PATH:="/var/lib/botkit"}

SORUCE="$(dirname $0)/../"
TARGET="$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH"
rsync -avz $SORUCE $TARGET
