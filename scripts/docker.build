#!/usr/bin/env bash

set -eu

OPTION=""

if [ $# -eq 1 ]; then
  case $1 in
  "clean")
    OPTION="--no-cache"
    ;;
  esac
fi

docker build \
  $OPTION \
  -t ${DOCKER_USERNAME}/${DOCKER_IMGNAME} \
  .
